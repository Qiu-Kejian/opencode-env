# Sprint <S> · <目标>

- 状态：planning / doing / done ｜ 输入：<卡 #N / 任务书路径 / FR / design 引用> ｜ 起点：<目录> ｜ 分支：`sprint/<S>`（产品仓库）｜ 日期：<YYYY-MM-DD>

## 接口冻结区

| 冻结项 | 签名 / schema 摘录 | 冻结时间 |
|---|---|---|
|  |  |  |

## 任务 DAG（状态事实源，每任务更新）

| 任务 | 名称 | 依赖 | 并行组 | 写范围 | 模型档 | 状态 | 在途 / 最后动作 |
|---|---|---|---|---|---|---|---|
| T-<S>-01 |  |  | G1 |  |  | todo | — |

## 恢复指引（leader 每次更新）

- 最后动作：
- 在途任务（未提交 / 已提交未过门）：
- 下一步：

## 批次门（机器验收）

- [ ] `python <编排目录>/tools/acceptance.py --sprint <S> --tasks <T-...>` → 退出码 0（报告 `runs/<S>-acceptance.md`）
- [ ] `<复核模型>`（异模型）复核报告 + 对抗抽检
- [ ] `[人工]` 项清单（无则写「无」；有则给可复现步骤）
- [ ] 页面 / 端到端冒烟（按项目门槛，可选）
- [ ] docs / PROGRESS 同步
- [ ] 看板同步（可选接入；机器 PASS 且无 `[人工]` → 完成，否则只列人工项）

## 结束报告（leader 回填）

- 完成 / blocked：
- 决策条目（decisions.md 编号）：
- 证据（runs/ 文件）：
- 提交哈希（产品仓库代码 hash；编排产物提交见实例仓库）：
- 机器验收：`runs/<S>-acceptance.md`（结论 PASS/FAIL）+ 复核结论
- 验收清单：`[机器]`（引用报告）/ `[人工]`（逐条可复现步骤；无则「无」）
